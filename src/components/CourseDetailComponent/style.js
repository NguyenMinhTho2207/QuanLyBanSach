import { Col } from "antd";
import { styled } from "styled-components";

export const WrapperBoxCourse = styled(Col) `
    display: flex;
    flex-direction: column;
    padding: 20px !important; 
    background: #fff; 
    border-radius: 10px; 
    box-shadow: 0 1px 4px 0 rgba(0,0,0,.12);
`